# WeChat ↔ Slack ブリッジ 動作ルール

## 概要
このプロジェクトはWeChatとSlackを双方向で連携するブリッジです。
Claude CodeがMCPサーバー経由でWeChat・Slackの両方を操作し、メッセージの転送と翻訳を行います。

## 構成
- **WeChat側**: wxauto4ベースのカスタムMCPサーバー（server.py）をWindows上で起動
- **Slack側**: 既存のSlack MCPサーバーを利用

## 翻訳ルール

- **WeChat → Slack**: 中国語のメッセージを**日本語**に翻訳して投稿
- **Slack → WeChat**: 日本語のメッセージを**中国語**に翻訳して送信
- 翻訳はClaudeが直接行う（外部API不要）
- 固有名詞・人名はそのまま保持する
- 意訳ではなく、原文のニュアンスを正確に伝える自然な翻訳を心がける

## Slackチャンネル命名規則

- WeChat連絡先ごとに個別のSlackチャンネルを使用する
- チャンネル名: `wechat-{連絡先名}` （例: `wechat-张三`, `wechat-李四`）
- 連絡先名はWeChatの表示名をそのまま使う（アルファベット小文字に変換）
- チャンネルが存在しない場合は新規作成する

## メッセージフォーマット

### WeChat → Slack 投稿時
```
【{連絡先名}】{タイムスタンプ}
{日本語翻訳テキスト}

> 原文: {中国語原文}
```

### Slack → WeChat 送信時
- Slackの投稿テキストを中国語に翻訳してWeChatで送信
- 翻訳のみ送信し、メタ情報は付加しない

## ポーリング動作

`/loop` コマンドで自動監視を開始する際の動作:

1. **WeChat新着チェック**: 各連絡先のチャットを開き、メッセージを取得
2. **差分検出**: 前回チェック時以降の新着メッセージを特定
3. **翻訳・転送**: 新着メッセージを日本語に翻訳し、対応するSlackチャンネルに投稿
4. **Slack返信チェック**: 各 `wechat-*` チャンネルの新着メッセージを取得
5. **返信転送**: Slackの新着返信を中国語に翻訳し、対応するWeChat連絡先に送信

## MCP ツール

### WeChat（server.py）
| ツール | 説明 |
|-------|------|
| `get_messages` | 指定連絡先のチャットを開き、表示中のメッセージを取得 |
| `send_message` | 指定連絡先にテキストメッセージを送信 |
| `send_file` | 指定連絡先にファイルを送信 |

## 注意事項

- WeChat操作中はWeChatウィンドウに手動で触れないこと（GUI自動操作のため）
- Slackの投稿はBot名義で行われる
- Python 3.12 + wxauto4 を使用（3.13では wxauto4 が未対応）
